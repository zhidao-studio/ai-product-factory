# 工程实施计划：[FEATURE]

**需求目录**：`[###-feature-name]`

**日期**：[DATE]

**状态**：草稿

**需求规格**：`specs/[###-feature-name]/spec.md`

> 前置门禁：`spec.md` 已由用户明确确认且需求校验通过。本文件只翻译已确认需求，
> 不得新增业务规则或借技术方案改变验收口径。

## Summary

[用一段话概括业务目标、受影响的真实工程和最小实现路径。]

## 需求追踪

| 需求编号 | 业务含义 | 工程响应 | 验证入口 |
|---|---|---|---|
| US-001 / BR-001 / AC-001 | [摘要] | [在哪个既有边界内实现] | [如何得到验收证据] |

## 当前事实与最近实现

- **现有能力**：[列出查阅过的真实 Controller、VO、模块、页面、SQL 或配置路径。]
- **最近实现**：[指出最接近的现有模式及复用方式。]
- **事实缺口**：[无，或列出必须研究且会影响方案的具体问题。]

结论必须来自仓库或官方一手资料。不得把猜测写成现状，也不得用未来目标替代当前事实。

## 影响范围

| 边界 | 是否涉及 | 影响与所有权 |
|---|---|---|
| Admin 后端 | [是/否] | [真实模块或“不涉及”] |
| Client 后端 | [是/否] | [真实模块或“不涉及”] |
| Common | [是/否] | [仅中立技术能力或“不涉及”] |
| PC Admin | [是/否] | [真实工程或“不涉及”] |
| H5 / App / 小程序 / HarmonyOS | [逐端填写] | [各自真实工程或“不涉及”] |
| 数据与迁移 | [是/否] | [数据所有者或“不涉及”] |
| 基础设施与文档 | [是/否] | [真实路径或“不涉及”] |

## Technical Context

仅填写受影响工程已经采用或经确认必须引入的内容；版本以 `docs/工程版本基线.md` 和真实清单为准。

- **语言与运行时**：[受影响端及当前版本；不涉及则写“不涉及”。]
- **框架与依赖**：[复用项、新增项及新增理由；无新增则明确写“无”。]
- **存储与状态**：[现有数据源、缓存或客户端状态；不涉及则写“不涉及”。]
- **认证与安全**：[沿用的真实策略和新增风险；不涉及则写“不涉及”。]
- **规模与性能约束**：[只有需求或现状提供依据时填写，否则写“无额外约束”。]

## 契约计划

- **现有契约依据**：[Controller、VO、请求层等真实路径。]
- **契约变化**：[逐项说明请求、响应、错误、鉴权和兼容性；不涉及则写“不涉及”。]
- **跨工程调用**：[调用方、所有者和信任边界；不涉及则写“不涉及”。]

只有契约真实变化时才创建 `contracts/`；不得用假接口或占位字段预建契约。

## 数据计划

- **数据所有者**：[Admin / Client / 目标前端 / 不涉及。]
- **模型或表变化**：[新增、变更、迁移和回滚；不涉及则写“不涉及”。]
- **一致性与生命周期**：[状态语义、并发、删除、审计和兼容影响；不涉及则写“不涉及”。]

只有数据结构或生命周期真实变化时才创建 `data-model.md`，并逐项对齐 `CLAUDE.md`。

## UI 与多端计划

- **用户流程**：[由 US-xxx 推导的页面和交互变化；不涉及则写“不涉及”。]
- **设计依据**：[已读取的设计系统与目标平台适配文档；不涉及则写“不涉及”。]
- **端间差异**：[各端独立实现与平台语义；不涉及则写“不涉及”。]
- **前后端对齐**：[真实后端 VO 与目标前端请求层；不涉及则写“不涉及”。]

## Project Structure

### 需求工件

```text
specs/[###-feature-name]/
├── spec.md
├── checklists/requirements.md
├── plan.md
└── tasks.md
```

`research.md`、`data-model.md`、`contracts/` 和 `quickstart.md` 按实际需要创建；不需要时不得创建空壳。

### 代码变更边界

```text
[只列本需求会修改或新增的真实目录和文件；删除未使用的占位内容。]
```

- **允许修改**：[精确到真实目录或文件。]
- **明确禁止**：[本需求不得触碰的相邻工程或边界。]
- **预计变更规模**：[文件数量级和原因，用于发现失控扩张。]
- **停止条件**：[发现哪些事实冲突、范围扩大或破坏性变化时必须回到用户确认。]

## 验证计划

| 验收标准 | 验证层级 | 方法 | 预期证据 |
|---|---|---|---|
| AC-001 | [业务路径/契约/构建/回归] | [可重复执行的步骤或命令] | [可观察结果] |

- **受影响构建**：[逐工程列出。]
- **关键业务路径**：[正常、权限、异常和边界场景。]
- **回归范围**：[必须保持不变的现有能力。]
- **环境限制**：[无，或如实列出不能验证的项目。]

## Constitution Check

- [ ] `spec.md` 已确认且只包含业务需求。
- [ ] 每个 US、BR、AC 都已映射到方案与验证。
- [ ] 方案基于真实代码和权威文件，没有发明契约或模块。
- [ ] Admin、Client、Common、数据与五端所有权保持正确。
- [ ] UI 变化已读取设计系统和对应平台规范。
- [ ] 附属工件均有真实内容需求，不创建空壳。
- [ ] 变更边界、风险、回归和停止条件已写清。

## Supporting Artifacts

| 工件 | 是否需要 | 生成条件与内容 |
|---|---|---|
| `research.md` | [是/否] | [仅用于真实且需要证据的技术决策。] |
| `data-model.md` | [是/否] | [仅当数据结构或生命周期变化。] |
| `contracts/` | [是/否] | [仅当外部或跨工程契约变化。] |
| `quickstart.md` | [是/否] | [仅当需要新的可复现运行或验收步骤。] |

## Complexity Tracking

仅在违反宪章或引入额外复杂度时填写；没有偏离时写“无”。

| 偏离 | 需求依据 | 为什么更简单的方案不成立 | 用户决定 |
|---|---|---|---|
| [无，或具体偏离] | [US/BR/AC] | [证据] | [待确认/已接受/已拒绝] |

## 计划确认

- **状态**：待用户确认
- **确认人**：[由用户或其明确指定的技术评审人填写]
- **确认依据**：[记录确认所在的任务、评审或链接]
- **确认范围**：需求映射、工程边界、契约/数据/UI 方案、验证方式和附属工件
- **说明**：确认本计划不等于授权实施；确认后才可生成 `tasks.md`。
