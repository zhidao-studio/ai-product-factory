# ============ OS / Editor ============
.DS_Store
.idea/
*.iml
*.ipr
*.iws
*.sw?
.vscode/
*.suo
*.sln

# ============ Logs ============
*.log
logs/

# ============ Build artifacts (忽略各端构建产物与依赖) ============
# 全局忽略，覆盖缺失 .gitignore 的子工程（如 web/miniapp）
node_modules/
dist/
dist-ssr/
build/
target/
.umi/
.umi-production/
src/.umi/
src/.umi-production/
.expo/
coverage/
Pods/
*.jsbundle

# ============ Local overrides ============
# 五个前端会提交不含真实生产密钥的环境模板，以保证复制后可直接开发。
# 生产部署从 infra/.env.prod.example 复制本地密钥文件，真实值禁止入库。
infra/.env.prod

# 本地 Agent 记忆不属于项目事实，不得进入提交。
.workbuddy/

# ============ Misc ============
*.tmp
*.bak
.cache/

# ============ 启动脚本产物 ============
backend/ruoyi-admin.pid
backend/ruoyi-client.pid
